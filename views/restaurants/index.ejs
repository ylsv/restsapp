<% include ../partials/header %>

<header class='jumbotron'>
    <div class="container">
        <h1>Приветствуем на нашем вкусном сайте!</h1>
        <p>Здесь можно узнать о лучших ресторанах или поделиться своими открытиями <i class="fas fa-utensils"></i></p>
        <p>
            <a class="btn btn-primary btn-lg" href="/restaurants/new">Добавить новый ресторан</a>
        </p>
    </div>
</header>

<div class="row text-center" style="display:flex; flex-wrap: wrap">
    <% restaurants.forEach(function(restaurant){ %>
        <div class="col-md-3 col-sm-6">
            <div class="thumbnail">
                <img src="<%= restaurant.image %>">
                <div class="caption">
                    <h4><%= restaurant.name %></h4>
                </div>
                <p>
                    <a href="/restaurants/<%= restaurant._id %>" class="btn btn-primary">Подробнее</a>
                </p>
            </div>
        </div>
    <% }); %>
</div>


<% include ../partials/footer %>